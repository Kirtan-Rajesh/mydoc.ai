# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111,<1
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # multipart/form-data file uploads
anyio>=4.0

# ── Configuration & validation ────────────────────────────────────────────────
pydantic>=2.7
pydantic-settings>=2.3

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.30             # async ORM
aiosqlite>=0.20                # SQLite async driver (dev / CI)
asyncpg>=0.29                  # Postgres async driver (production)

# ── Auth ──────────────────────────────────────────────────────────────────────
PyJWT>=2.8

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx>=0.27                    # Gemini REST, MSG91, Twilio API calls

# ── Document processing ───────────────────────────────────────────────────────
pypdf>=4.2                     # Text extraction from digital PDFs

# ── File storage ──────────────────────────────────────────────────────────────
boto3>=1.34                    # S3-compatible storage (AWS S3, Supabase, R2, MinIO)
google-cloud-storage>=2.16     # GCS backend (optional — only needed if STORAGE_BACKEND=gcs)

# ── Monitoring ────────────────────────────────────────────────────────────────
sentry-sdk[fastapi]>=2.0
